# Legacy scripts

One-off scripts used during earlier rounds of question authoring and bulk fixes.
Kept for historical reference; not part of the application runtime.

Run them at your own risk — paths are relative to the repo root and they may
mutate `questions/*.json` in place.
